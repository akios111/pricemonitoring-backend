## Optional: enable real emails via SendGrid
## 1) Set SPRING_PROFILES_ACTIVE=dev,default in docker-compose.dockerhub.yml (or backend.yml)
## 2) Fill these values

# Required for email sending
SEND_GRID_API_KEY=

# Optional: override the From address (must be verified in SendGrid)
SEND_GRID_FROM=
